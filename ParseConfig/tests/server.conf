server {
    listen    80;
    server_name    www.example.com example.com;

    location /redirect {
        rewrite /this_is_a_redirection;
    }

    location / {
        include proxy_params;
        proxy_pass http://backend_apache;
    }
}